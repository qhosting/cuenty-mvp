
# =============================================================================
# CUENTY MVP - Configuración de Variables de Entorno
# =============================================================================
# IMPORTANTE: Copia este archivo a .env y actualiza los valores según tu entorno
# NUNCA subas el archivo .env a repositorios públicos
# =============================================================================

# -----------------------------------------------------------------------------
# CONFIGURACIÓN GENERAL
# -----------------------------------------------------------------------------
NODE_ENV=production
PORT=3000

# -----------------------------------------------------------------------------
# BASE DE DATOS POSTGRESQL
# -----------------------------------------------------------------------------
DB_HOST=postgres
DB_PORT=5432
DB_NAME=cuenty_db
DB_USER=cuenty_user
DB_PASSWORD=cambiar_password_seguro_aqui

# -----------------------------------------------------------------------------
# SEGURIDAD
# -----------------------------------------------------------------------------
# JWT Secret - Cambiar por una clave aleatoria fuerte (mínimo 32 caracteres)
JWT_SECRET=cambiar_este_secreto_por_uno_aleatorio_y_seguro_minimo_32_caracteres

# Encryption Key - Clave para encriptar credenciales de cuentas (mínimo 32 caracteres)
ENCRYPTION_KEY=cambiar_esta_clave_de_encriptacion_por_una_aleatoria_segura_32_chars

# -----------------------------------------------------------------------------
# WEBHOOKS N8N
# -----------------------------------------------------------------------------
# Secret compartido para autenticar webhooks de n8n
N8N_WEBHOOK_SECRET=cambiar_secret_compartido_con_n8n

# URLs de webhooks de n8n (dejar vacío si no se usa n8n aún)
N8N_WEBHOOK_ENTREGA_CUENTA=https://tu-n8n.com/webhook/notificar-entrega-cuenta
N8N_WEBHOOK_RESPUESTA_AGENTE=https://tu-n8n.com/webhook/respuesta-agente

# -----------------------------------------------------------------------------
# CORS (Control de Acceso)
# -----------------------------------------------------------------------------
# En producción, especifica el dominio exacto. Ejemplo: https://cuenty.com
# En desarrollo local, usar * está bien
CORS_ORIGIN=*

# -----------------------------------------------------------------------------
# EJEMPLOS DE CONFIGURACIÓN PARA DIFERENTES ENTORNOS
# -----------------------------------------------------------------------------

# DESARROLLO LOCAL:
# NODE_ENV=development
# DB_HOST=localhost
# CORS_ORIGIN=*

# PRODUCCIÓN (Easypanel):
# NODE_ENV=production
# DB_HOST=postgres (nombre del servicio en docker-compose)
# CORS_ORIGIN=https://tu-dominio.com
